name: CI
on:
    push:
        branches: ["main"]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: print my name 
              run : echo Aman kumar rathi

            - name : print try 
              run  : echo again 